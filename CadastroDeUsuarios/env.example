# ==========================================================
# VARIÁVEIS DE AMBIENTE - EXEMPLO DE CONFIGURAÇÃO (H2)
#
# CRIE UMA CÓPIA DESTE ARQUIVO E RENOMEIE-A PARA .env
# PREENCHA COM SUAS CREDENCIAIS REAIS E LOCAIS.
# ==========================================================

# 1. URL de Conexão com o H2 (Modo Arquivo)
# Se você usa o H2 em modo arquivo, a URL deve apontar para o diretório de dados.
# Exemplo: jdbc:h2:~/nome_do_seu_banco;
DATABASE_URL=jdbc:h2:~/nome_do_seu_banco;

# 2. Usuário de Acesso ao Banco de Dados
# O padrão do H2 para teste é 'sa' (System Administrator).
DATABASE_USERNAME=sa

# 3. Senha de Acesso ao Banco de Dados
# O padrão do H2 é não ter senha (deixe em branco) ou use a sua, se configurada.
DATABASE_PASSWORD=